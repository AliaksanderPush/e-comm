import React from 'react';
import {
  Box,
  ScrollView,
  HStack,
  Text,
  Select,
  Center,
  VStack,
  Image,
  Pressable,
} from 'native-base';

export const VideoDetailsScreen = ({ route }) => {
  return (
    <Center>
      <Text>VideoDetails</Text>
    </Center>
  );
};
